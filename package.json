{
  "name": "authentication",
  "version": "1.0.0",
  "description": "serving page with authentication",
  "main": "Server/client_server.js",
  "scripts": {
    "start": "cd Server && nodemon client_server.js",
    "startAuth": "cd Server && nodemon auth_server.js",
    "build": "cd client && npm run build && cd .. & npm start"
  },
  "author": "Pat Cannon",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "body-parser": "^1.20.2",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "nedb": "^1.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.21"
  }
}
